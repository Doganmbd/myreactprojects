import React from 'react'

const Button = () => {
  return (
    <div className="btn">
      <button  style={{backgroundColor:"red",color:"white" }}>Add Task tracker </button>
    </div>
  )
}

export default Button